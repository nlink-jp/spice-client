--- a/guest/animation-load.sh
+++ b/guest/animation-load.sh
@@ -3,4 +3,4 @@
-exec xterm \
+exec setsid xterm \
     -fullscreen \
     -bg black \
     -fg white \
--- a/guest/init
+++ b/guest/init
@@ -61,7 +61,7 @@
     pid_file="$1"
     if test -f "${pid_file}"; then
         pid="$(cat "${pid_file}")"
-        kill "${pid}" 2>/dev/null || true
+        kill -TERM "-${pid}" 2>/dev/null || kill "${pid}" 2>/dev/null || true
         wait "${pid}" 2>/dev/null || true
         rm -f "${pid_file}"
     fi
